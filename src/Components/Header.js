import React, { Component } from 'react';

const Header = (props) => {
        return(
            <div className="head">
                <h1>{props.title}</h1>
            </div>
            
        );
    

};

export default Header;
